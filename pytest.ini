[pytest]
# Automatically detect async test functions without requiring @pytest.mark.asyncio
asyncio_mode = auto

# Set default event loop scope to avoid pytest-asyncio scope warnings
asyncio_default_fixture_loop_scope = function
